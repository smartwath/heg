---
trigger: always_on
---

# Angular CLI Rules

Always use Angular CLI generators when creating Angular artifacts.

Examples:

ng generate component
ng generate service
ng generate guard
ng generate interceptor
ng generate pipe
ng generate directive
ng generate interface
ng generate class

Do not manually create Angular files when an Angular CLI generator exists.

Never use:

--skip-tests

Tests must be generated by default.